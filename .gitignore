# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*

# environment
.env
.env.production

# editor / os
.DS_Store

# reference doc, not part of the site — kept local only (11MB binary)
Brand Board*.pdf

# Claude Code project/session files — dev-tool config, not part of the site
.claude/

# personal editing notes — not part of the deployed site, kept local only
EDITING.md

# developer-facing technical deep dive — local reference only, not published
TECHNICAL.md
